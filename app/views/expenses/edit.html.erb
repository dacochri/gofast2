<section>
  <h2>Edit expense #<%= @expense.id %></h2>
  <%= render 'form' %>
  <p><%= link_to 'Show', @expense %> | <%= link_to 'Back', expenses_path, :class => 'ym-button ym-close' %></p>
</section>
