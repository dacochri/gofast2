<section>
  <h2>Edit Announcement #<%= @announcement.id %></h2>
  <%= render 'form' %>
  <p><%= link_to 'Show', @announcement, :class => 'button show' %><%= link_to 'Back', announcements_path, :class => 'button back' %></p>
</section>
