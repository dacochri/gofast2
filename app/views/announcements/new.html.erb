<!-- new announcement form -->
<section>
  <h2>New Announcement</h2>
  <!-- load announcement from _form.html.erb -->
  <%= render 'form' %>
  <p><%= link_to 'Back', announcements_path, :class => 'button back' %></p>
</section>
