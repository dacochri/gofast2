<section>
  <h2>Trailers</h2>
  <table>
    <thead>
      <tr>
        <th scope="col">Trailer no</th>
        <th scope="col">Trailer type</th>
        <th scope="col">License plate</th>
        <th scope="col">Make</th>
        <th scope="col">Model</th>
        <th scope="col">Manufactured year</th>
        <th scope="col">Vin number</th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody>
    <% @trailers.each do |trailer| %>
      <tr>
        <td><%= trailer.trailer_no %></td>
        <td><%= trailer.trailer_type %></td>
        <td><%= trailer.license_plate %></td>
        <td><%= trailer.make %></td>
        <td><%= trailer.model %></td>
        <td><%= trailer.manufactured_year %></td>
        <td><%= trailer.vin_number %></td>
        <td><%= link_to '', trailer, :class => 'button show', :title => 'Show' %> <%= link_to '', edit_trailer_path(trailer), :class => 'button edit', :title => 'Edit' %> <%= link_to '', trailer, method: :delete, data: { confirm: 'Are you sure you want to delete this trailer?' }, :class => 'button destroy', :title => 'Delete' %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
  <p><%= link_to 'New Trailer', new_trailer_path, :class => 'button new' %></p>
</section>
